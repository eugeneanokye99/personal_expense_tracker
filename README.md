
  # Eugene's Expense Tracker

  This is a code bundle for Eugene's Expense Tracker. The original project is available at https://www.figma.com/design/sBKAVqHqewNOOh0rwSKCP8/Eugene-s-Expense-Tracker.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  